{% extends 'base.html.twig' %}

{% block pageTitle %}Offers{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center ">
            {% for ride in rides %}
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">Depart: {{ ride.depart }}</h5>
                            <p class="card-text">Destination: {{ ride.destination }}</p>
                            <p class="card-text">Places Dispo: {{ ride.seats }}</p>
                            <p class="card-text">Prix: {{ ride.price }} €</p>
                            {# <p class="card-text">{{ ride.date }}</p> #}
                            {# <p class="card-text">{{ ride.rules }}</p>  #}
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
